package com.plat.acoal.utils;
import java.text.NumberFormat;
import java.text.DecimalFormat;

public class NumberUtil {

    /**
     * 将小数变成百分数
     */
     public static String DecimalToPercent(double f){
          String percent=(Integer.parseInt(new DecimalFormat("0").format(f*100))+"%");
          System.out.println(Integer.parseInt(new DecimalFormat("0").format(f*100))+"%");
          return  percent;
     }
}
